<h2>Favorite people</h2>

<% if @favorites_printable.length >= 1 %>
  <div class="printable-list">
    <% @favorites_printable.each_with_index do |favorite, index| %>
      <div class="printable-list-item">
        <h3>
          <%= index + 1 %>. <%= favorite["first_name"].nil? ? favorite["tiss_id"] : favorite["first_name"] + ' ' + favorite["last_name"] %>
        </h3>

        <% if @with_annotation %>
          <p>
            Note: <%= (@annotation.find_by("annot_id" => favorite['tiss_id']) && !@annotation.find_by("annot_id" => favorite['tiss_id'])&.annotation.empty?) ? @annotation.find_by("annot_id" => favorite['tiss_id']).annotation : "Empty" %>
          </p>
        <% end %>


      </div>
    <% end %>
  </div>
<% else %>
  <p class="page-title"> You don't have any favorites. </p>
<% end %>